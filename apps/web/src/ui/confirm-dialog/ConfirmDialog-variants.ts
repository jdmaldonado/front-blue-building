import { cva } from 'class-variance-authority';

export const confirmDialogTextVariants = cva('text-body text-(--text-secondary)');

export const confirmDialogFooterVariants = cva('flex w-full justify-end gap-2');
