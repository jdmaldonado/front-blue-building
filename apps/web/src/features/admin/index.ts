export { AdminQuickSearch } from './AdminQuickSearch';
export { BuildingsOverviewPage } from './BuildingsOverviewPage';
export { BuildingSummaryPage } from './BuildingSummaryPage';
// The route owns the shell, so it needs the pieces that go in its header.
export { BuildingHeader, BuildingSectionNav } from './components';
